pipeline {
    agent any

    stages {
        stage('docker') 
        {
            steps {
                sh 'docker build -t balu:latest .'
                sh 'docker run -d -p 81:80 --name balu1 balu:latest'
            }
        }
    }
  }
