pipeline {
    agent any

    stages {
        stage('docker')
        {
            steps {
                sh 'docker build -t abcd:latest .'
                sh 'docker run -d -p 81:80 --name anji abcd:latest'
            }
        }
    }
  }

